/**
 * Step 7: Replace with paginated scan + SQLite cache.
 */
export async function calculateStorageUsedBytes(): Promise<number> {
  // TODO(Step 7): Aggregate asset sizes from cached metadata
  return 0;
}

/**
 * Step 7: Replace with category analysis (screenshots, duplicates, large videos).
 */
export async function calculatePotentialSavingsBytes(): Promise<number> {
  // TODO(Step 7): Sum estimated savings from quick-clean categories
  return 0;
}
